[project]
name = "graphlaplacianoptimizer"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "arrowspace>=0.25.14",
    "numpy>=2.4.2",
    "optuna>=4.7.0",
]

[project.optional-dependencies]
dev = [
    "optuna-dashboard>=0.17.0", # optional: visual trial monitoring
    "pytest>=9.0.2", 
]

[project.scripts]
optimize = "graphlaplacianoptimizer._optimizer:main"



